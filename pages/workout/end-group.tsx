import { IWorkout } from '../api/workouts';
import { stateType, IStateProps } from './[id]';

export interface EndGroupProps extends IStateProps {}
export function EndGroup(props: EndGroupProps) {
  return <div>EndGroup</div>;
}
